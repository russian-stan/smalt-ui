# pnpm settings
# Strict hoisting: dependencies do not leak between packages, as in a real publish.
shamefully-hoist=false
# Install peer dependencies automatically (vue, reka-ui, etc.).
auto-install-peers=true
# One version of shared dependencies across the whole workspace.
save-workspace-protocol=rolled
